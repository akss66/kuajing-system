<!-- SEED: re-run $impeccable document once there is UI code to capture actual tokens and components. -->
---
name: 同舟行跨境
description: 面向加拿大 TEMU 一件代发业务的清晰、高效运营系统
---

# Design System: 同舟行跨境（共享基础）

实现任何界面前必须先按受众读取对应规范：

- 管理端：`DESIGN.admin.md`
- 客户端：`DESIGN.portal.md`

本文件只定义两端共享的品牌、字体、可访问性和事实一致性；两端不再共享同一套页面构图。

## Overview

**Creative North Star: “同舟行商家工作台”**

界面像一套成熟的跨境商家中心：深炭黑全局工具顶栏、浅色业务导航、浅冷灰工作画布和白色数据面板形成稳定骨架。用户能快速辨认订单、库存、资金和异常。设计服务于高频操作，不通过装饰证明高级感。

管理员界面中等偏紧凑；客户门户拥有独立的色彩、控件质感和页面构图。两端可以共享底层无障碍组件与业务状态，但不得因为复用组件而看成同一套后台换文案。移动端必须重新组织表格信息和操作顺序，禁止把桌面页面等比缩小。

视觉与交互参考 shadcn/ui、Next Shadcn Dashboard Starter、Saleor Dashboard 和飞书的成熟模式，但不复制任何模板主题。平台只提供浅色主题。

**Key Characteristics:**

- 专业、可靠、高效
- 商家中心式、清楚、克制
- 状态优先于装饰
- 桌面高密度与移动端易触控并存
- 动效只解释状态变化

TikTok Shop 商家中心只作为 UI 美学和组件质感参考。系统必须保留同舟行自己的信息架构、业务命名、权限、菜单和流程，不复制参考产品的栏目或业务模型。

## Colors

**The Restrained Teal Rule.** 深海青绿是主操作和选中状态的唯一产品主色，在单屏视觉面积中保持克制。主色不得演变成大面积彩色背景。

页面使用深炭黑、纯白和冷灰中性色建立层级。深炭黑只用于全局工具顶栏和选中导航等有限区域，不代表深色主题。Logo 红仅属于品牌标志本身，不用于主按钮、导航选中或链接。成功、警告、错误和信息状态使用独立语义色，并辅以图标或文字，禁止只靠颜色传达状态。

真实设计令牌应包含深炭黑顶栏、浅冷灰画布、白色面板、深海青绿主操作和独立语义色阶。

## Typography

采用统一的本地可变无衬线产品字体体系：中文字符优先使用 `Noto Sans SC Variable`，拉丁字母、数字和西文标点优先使用 `Geist Variable`。两套字体资源随项目本地打包，不依赖运行时第三方 CDN。正文、标题、按钮、表单、表格、浮层和图表共享同一全局字体令牌，禁止页面或业务组件单独覆盖 `font-family`；金额、库存和编号启用等宽数字特性以保持列对齐。

字体层级紧凑、稳定，不使用展示字体、手写字体或夸张的大标题。表格正文不得低于 13px，移动端主要操作正文不得低于 14px。

**The Data Alignment Rule.** 金额、数量、日期和单号必须通过对齐、字重和等宽数字形成可扫读结构，不能依赖彩色标签弥补排版混乱。

## Elevation

界面默认扁平，通过深色顶栏、纯白、浅冷灰表面、细分隔线和间距建立层级。静态面板使用 4–8px 小圆角；阴影仅用于浮层、下拉菜单、抽屉和确实脱离页面的临时元素，且模糊范围保持克制。

**The Flat-by-Default Rule.** 静态页面容器不同时使用边框和宽模糊阴影；如果页面看起来由许多漂浮卡片堆成，层级设计就是错误的。

## Components

组件以 shadcn/ui 的开放组件模式为基础，但颜色、密度、中文文案和状态行为必须由同舟行跨境自己的设计令牌控制。

- 按钮、输入框和筛选器采用清楚的 8px 左右圆角，不使用胶囊式主按钮。
- 主操作使用深海青绿实色和白色文字；次操作使用中性边框或文字按钮。
- 数据表格支持固定关键列、筛选、排序、批量选择、加载骨架、空状态和错误恢复。
- 手机端的数据表按任务改造成信息分组、横向浏览或主次字段折叠，不能缩小字号硬塞所有列。
- 大部分状态切换使用 150–200ms 的快速过渡，并为 `prefers-reduced-motion` 提供即时或淡入替代。

## Do's and Don'ts

### Do:

- **Do** 使用深海青绿表示主要动作、当前选中和键盘焦点。
- **Do** 使用深炭黑全局工具顶栏、浅色左侧导航和浅冷灰工作画布建立商家中心气质。
- **Do** 保留同舟行已经确认的信息结构和业务流程，只更新 UI 美学和组件表达。
- **Do** 保持中等偏紧凑的信息密度，让管理员能在一屏处理较多订单和 SKU。
- **Do** 为客户门户提供至少 44px 的关键触控目标，并以真实手机宽度验收。
- **Do** 为默认、悬停、焦点、按下、禁用、加载、成功和错误状态提供一致反馈。
- **Do** 使用清晰文案解释错误原因、未受影响的数据和下一步操作。

### Don't:

- **Don't** 使用 Logo 红色作为主操作色，也不要采用大面积红色界面。
- **Don't** 复制 TikTok Shop 的栏目、导航顺序、角色或业务命名。
- **Don't** 制作黑色科技大屏或深色主题；第一期只提供浅色主题。
- **Don't** 使用花哨渐变、玻璃拟态、装饰性动画或视觉噪声。
- **Don't** 使用过度圆角、满屏悬浮卡片和层层嵌套卡片。
- **Don't** 为了所谓高级感留下妨碍高频操作的大面积空白。
- **Don't** 直接套用通用 SaaS 模板主题或让模板业务模型决定页面结构。
