---
name: 同舟行客户拿货中心
audience: CUSTOMER
---

# Design System: 客户端

## Creative North Star

“可信的拿货航线”。客户不需要理解后台结构，只需要清楚知道：现在有什么货、下一步要做什么、钱在哪里、订单走到哪里。界面应比管理端更温润、更有呼吸感，但仍保持专业、准确和高信息密度。

## Color strategy

采用“可信的现代商家客户端”：精确使用 `#F4F5F7` 冷中性画布、纯白内容表面、深云杉顶栏和深海青绿主操作。画布与白色表面的稳定色差是第一层空间关系；`brand-50` 浅青绿只用于当前路径、重点提示和选中态，`brand-700` 用于对应文字。琥珀只表示付款或待处理，红色只表示错误和需要协助，绿色只表示完成与可售。颜色用于路径和状态，不做装饰性铺色；禁止渐变、玻璃拟态和 Logo 红主按钮。

客户静态主表面使用 `0 2px 12px rgb(0 0 0 / 2%)` 的微弱弥散投影形成接近 Notion、Linear 的安静层级；常规卡片不再依赖灰色实线边框。只有输入、分段控件和确需表达边界的复合组件可以使用极浅边框。侧栏依靠背景色差分区，不使用强硬竖线。此规则仅适用于客户端，管理端继续遵循自己的高密度扁平规范。

客户端桌面外壳使用从顶部到底部的 `256px` 浅灰侧栏，不保留管理后台式的横向顶栏；品牌位于侧栏顶部，账号入口固定在侧栏底部。移动端才使用紧凑顶栏和抽屉导航。页面标题是画布上的安静标题区，不再放进彩色 KPI 卡片；核心任务使用一条浅品牌色行动横幅，统计只在确有决策价值时出现。

## Spatial thesis

客户首页的主路径是“继续当前任务 → 查看货盘或上传 → 查询订单与资金”。主继续任务是页面视觉领导者，待办概览是支持信息，店铺和资金是安静摘要。避免同尺寸白卡平均分配注意力。

- 桌面：主任务与待办并列；快捷拿货横向成组；店铺摘要与资金摘要双栏。
- 手机：严格按任务顺序单列；固定底部导航保留首页、上传、订单、资金四个最高频入口。
- 客户端不使用管理后台式数据表格。货盘、订单、资金和上传记录统一采用独立列表卡片，并通过信息分组、状态徽标和渐进披露控制密度；不能同时渲染桌面表格和移动卡片两套完整大列表。
- 多店铺上传页只允许一个视觉主任务：有未完成上传时显示“继续/放弃”，没有时才显示“开始批量上传”；同一记录不能在主任务区和列表中重复占位。
- 上传、订单等线性任务默认控制在约 1024px 的专注阅读宽度；客户首页、个人中心和资金中心使用约 1360px 的标准工作区，实时货盘使用更宽的浏览工作区。不能让所有页面都继承管理端的超宽数据画布，也不能用过窄容器浪费桌面空间。

## Components

- 主按钮高 44–48px、深海青绿实色、白字、轻微内高光与明确按压位移。
- 次按钮使用纸白表面和精细边框；相邻筛选动作必须形成一个工具栏体系，不能像独立默认按钮拼接。
- 搜索、排序和状态筛选组成同一任务工具条：搜索是主动作，排序是中性控件，状态使用实线分段选择；禁止虚线按钮和漂浮的孤立按钮。
- “草稿”等内部实现词不作为客户主动作名称；用“开始上传、继续上传、上传记录”表达客户正在完成的任务。
- 步骤条必须明确区分已完成、当前和未来步骤：已完成使用勾选状态，当前使用品牌色实心序号，未来使用白底灰字，禁止勾号与数字混用导致阶段不清。
- 上传区域优先使用完整宽度的拖拽面板和明确的文件约束；店铺选择、文件状态与移除动作必须保持同一视觉轴线，不能形成左空右挤的失衡布局。
- 主表面统一使用 `rounded-2xl`，行级卡片使用 `rounded-xl`；避免无意义的三层嵌套圆角。
- 侧栏选中态是左右留白的圆角胶囊，不得使用贴边的全宽色块；侧栏仅允许极浅分隔或纯背景色差。
- 空状态必须包含浅灰图标容器、清晰标题、弱化说明和必要时的下一步动作，禁止只显示一行居中文字。
- 浏览器滚动条使用细窄、低对比轨道和悬停加深的客户端样式，同时保留键盘和触摸滚动能力。
- 实时货盘在桌面端按商品分组、按 SKU 单行横向排列；图片、SKU、规格、价格、库存和状态必须处于稳定列位。禁止两列大 SKU 卡片和同一商品链接在每个 SKU 行重复占据大面积空间；移动端改为紧凑纵向信息块。
- 金额、库存、数量和单号使用等宽数字；长单号和文件名必须可换行或保留完整可访问名称。
- 每个异步流程必须覆盖加载、空、失败、重试、重复提交和成功反馈。

## Delight

只允许与业务相关的一次性微动效：无待办时完成标记轻微落定，或主路径箭头在悬停时前移。动效不得延迟任务；`prefers-reduced-motion` 下立即完成。

客户端动效采用“操作回声”原则：页面切换只做短暂内容聚焦；当前导航图标轻微落定；按钮按下、主路径箭头、文件拖入和文件就绪提供即时反馈。常规反馈控制在 140–220ms，浮层不超过 280ms，使用自然减速曲线。禁止全页依次飞入、滚动触发动画、循环呼吸、弹跳和大幅悬浮；移除某个动效后如果不影响状态理解，就不应加入。

## Hardening

- 客户关键点击目标至少 44×44px，移动输入字号至少 16px以避免 iOS 自动缩放。
- 提供跳过导航入口、清晰焦点、动态筛选结果播报和门户级加载/错误/404恢复页。
- 付款金额、库存和状态必须与后端合同一致；不可编辑的精确金额不得表现为自由输入。
- 资金历史、订单和货盘如被截断，必须显示范围并提供分页或继续加载，不得用“全部/每一笔”误导。
- 删除只能作用于本客户尚未提交的上传内容，并需要确认弹窗、租户校验和审计；部分或全部已提交的记录必须保留且不可删除。
